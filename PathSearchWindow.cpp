#include "PathSearchWindow.h"

#include "Storage.h"

PathSearchWindow::PathSearchWindow(QWidget *parent)
	: QWidget(parent)
{
	ui.setupUi(this);
	
	//connect(ui.fromLineEdit,SIGNAL(ui.fromLineEdit->editingFinished()),this,)

}

PathSearchWindow::~PathSearchWindow()
{
}
